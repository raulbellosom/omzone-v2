# ── Built-in (auto-injected by Appwrite at runtime) ──
# APPWRITE_FUNCTION_API_ENDPOINT=
# APPWRITE_FUNCTION_PROJECT_ID=
# x-appwrite-key header (dynamic, not an env var)

# ── Project-level globals ──
# APPWRITE_DATABASE_ID=omzone_db
# APPWRITE_COLLECTION_CONTACT_MESSAGES=contact_messages

# ── reCAPTCHA ──
# RECAPTCHA_SECRET_KEY=   ← Server-side secret. NEVER expose in frontend.
