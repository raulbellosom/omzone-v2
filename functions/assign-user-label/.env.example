# ─── Variables used by this function (reference only) ───
# Built-in (auto-injected by Appwrite, no config needed):
#   APPWRITE_FUNCTION_API_ENDPOINT
#   APPWRITE_FUNCTION_PROJECT_ID
#   x-appwrite-key header (dynamic API key at runtime)
#
# Project-level global variables (set in Appwrite Project Settings):
APPWRITE_DATABASE_ID=omzone_db
APPWRITE_COLLECTION_USER_PROFILES=user_profiles
